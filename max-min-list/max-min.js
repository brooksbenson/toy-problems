//create two functions: max and min

//max returns the largest number from an array of integers
//min returns the smallest number from an array of integers
const max = ints => Math.max(...ints);
const min = ints => Math.min(...ints);